# GrandMA3 Cue Display Configuration
# Copy this file to .env and modify as needed

# Web server port (default: 3000)
HTTP_PORT=3000

# OSC listening port (default: 8000)
# This must match the port configured in GrandMA3 OSC settings
OSC_PORT=8000
